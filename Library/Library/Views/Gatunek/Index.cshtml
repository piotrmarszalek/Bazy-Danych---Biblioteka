@model IEnumerable<Library.Models.Gatunek>

@{
    ViewBag.Title = "Index";
}

<h2>Gatunki</h2>

<p>
    @Html.ActionLink("Dodaj nowy", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazwa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nazwa)
        </td>
        <td>
            @Html.ActionLink("Edytuj", "Edit", new { id=item.GatunekId }) |
            @Html.ActionLink("Usuń", "Delete", new { id=item.GatunekId })
        </td>
    </tr>
}

</table>
